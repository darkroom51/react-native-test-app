import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  errorContainer: {
    height: 100,
    width: '100%',
    padding: 20,
    backgroundColor: 'red',
    alignItems: 'center',
    justifyContent: 'flex-start'
  },
  errorMessage: {
    color: '#fff',
    paddingBottom: 20
  }
});